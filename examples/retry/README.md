# Retry Examples

This directory contains examples for using the retry package, which provides
configurable retry logic with backoff strategies.

## Examples

- `main.go` - Default retry configuration, custom retry settings, and error handling

## Running the Examples

```bash
go run main.go
```
